{
  "name": "awk",
  "scripts": {
    "bats": "cd $INIT_CWD && bats",
    "shellcheck": "shellcheck",
    "lint": "shellcheck ./exercises/**/*.sh",
    "test": "bats ./exercises/**/*.bats"
  },
  "dependencies": {
    "bats": "^1.10.0",
    "shellcheck": "^2.2.0"
  }
}
